my_f(x, y) = 2x + 3y
# If this function gets changed, it has to be reloaded.
# In Juno, we can just evaluate it another time. If we don't want to have to
# do that, we could also make use of Revise.jl, which reloads the files, when
# the containing files are saved.
